1656c1656
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Kein Filter` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='69' startCol='13' endLine='70' endCol='22' path="$.paths./stations.json.get.parameters[3].content.waters.schema.examples.Kein Filter.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">67</span><span class="cl"><span class="w">          </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Gewässer, filtert Stationen für bestimmte Gewässer&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Rhein` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='69' startCol='13' endLine='73' endCol='17' path="$.paths./stations.json.get.parameters[3].content.waters.schema.examples.Rhein.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">67</span><span class="cl"><span class="w">          </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Gewässer, filtert Stationen für bestimmte Gewässer&#34;</span><span class="w">
1663c1663
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Rhein` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='69' startCol='13' endLine='73' endCol='17' path="$.paths./stations.json.get.parameters[3].content.waters.schema.examples.Rhein.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">67</span><span class="cl"><span class="w">          </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Gewässer, filtert Stationen für bestimmte Gewässer&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Kein Filter` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='69' startCol='13' endLine='70' endCol='22' path="$.paths./stations.json.get.parameters[3].content.waters.schema.examples.Kein Filter.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">67</span><span class="cl"><span class="w">          </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Gewässer, filtert Stationen für bestimmte Gewässer&#34;</span><span class="w">
1677c1677
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Kein Filter` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='91' startCol='13' endLine='92' endCol='22' path="$.paths./stations.json.get.parameters[6].content.fuzzyId.schema.examples.Kein Filter.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">89</span><span class="cl"><span class="w">            </span><span class="nt">type</span><span class="p">:</span><span class="w"> </span><span class="l">string</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Berlin` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='91' startCol='13' endLine='94' endCol='22' path="$.paths./stations.json.get.parameters[6].content.fuzzyId.schema.examples.Berlin.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">89</span><span class="cl"><span class="w">            </span><span class="nt">type</span><span class="p">:</span><span class="w"> </span><span class="l">string</span><span class="w">
1684c1684
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Berlin` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='91' startCol='13' endLine='94' endCol='22' path="$.paths./stations.json.get.parameters[6].content.fuzzyId.schema.examples.Berlin.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">89</span><span class="cl"><span class="w">            </span><span class="nt">type</span><span class="p">:</span><span class="w"> </span><span class="l">string</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Kein Filter` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='91' startCol='13' endLine='92' endCol='22' path="$.paths./stations.json.get.parameters[6].content.fuzzyId.schema.examples.Kein Filter.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">89</span><span class="cl"><span class="w">            </span><span class="nt">type</span><span class="p">:</span><span class="w"> </span><span class="l">string</span><span class="w">
1859c1859
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Pegelnummer` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='314' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[0].content.station.schema.examples.Pegelnummer.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `UUID` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='309' endCol='18' path="$.paths./stations/{station}.json.get.parameters[0].content.station.schema.examples.UUID.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
1866c1866
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Name` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='312' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[0].content.station.schema.examples.Name.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `UUID` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='309' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[0].content.station.schema.examples.UUID.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
1873c1873
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Name` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='312' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[0].content.station.schema.examples.Name.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Name` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='312' endCol='18' path="$.paths./stations/{station}.json.get.parameters[0].content.station.schema.examples.Name.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
1880c1880
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `UUID` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='309' endCol='18' path="$.paths./stations/{station}.json.get.parameters[0].content.station.schema.examples.UUID.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `UUID` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='309' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[0].content.station.schema.examples.UUID.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
1887c1887
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Name` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='312' endCol='18' path="$.paths./stations/{station}.json.get.parameters[0].content.station.schema.examples.Name.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Name` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='312' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[0].content.station.schema.examples.Name.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
1901c1901
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `UUID` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='309' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[0].content.station.schema.examples.UUID.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Pegelnummer` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='314' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[0].content.station.schema.examples.Pegelnummer.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
1922c1922
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Pegelnummer` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='314' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[0].content.station.schema.examples.Pegelnummer.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Pegelnummer` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='314' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[0].content.station.schema.examples.Pegelnummer.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
1929c1929
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `UUID` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='309' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[0].content.station.schema.examples.UUID.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Pegelnummer` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='314' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[0].content.station.schema.examples.Pegelnummer.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
1936c1936
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Pegelnummer` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='314' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[0].content.station.schema.examples.Pegelnummer.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Name` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='312' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters
eit` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='352' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Windgeschwindigkeit.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Trübung` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='350' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Trübung.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2174c2174
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Windrichtung` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='354' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Windrichtung.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Niederschlag` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='356' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Niederschlag.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2181c2181
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Niederschlag` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='356' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Niederschlag.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Signifikante Wellenhöhe` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='362' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Signifikante Wellenhöhe.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2188c2188
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Chlorid` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='372' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Chlorid.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Windgeschwindigkeit` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='352' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Windgeschwindigkeit.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2195c2195
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Niederschlagsintensität` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='358' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Niederschlagsintensität.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Windgeschwindigkeit` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='352' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Windgeschwindigkeit.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2202c2202
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Wellenperiode` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='360' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Wellenperiode.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Windrichtung` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='354' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Windrichtung.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2209c2209
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Signifikante Wellenhöhe` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='362' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Signifikante Wellenhöhe.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Niederschlag` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='356' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Niederschlag.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2216c2216
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Maximale Wellenhöhe` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='364' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Maximale Wellenhöhe.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Niederschlagsintensität` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='358' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Niederschlagsintensität.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2223c2223
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Sauerstoffgehalt` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='366' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Sauerstoffgehalt.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Wellenperiode` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='360' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Wellenperiode.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2230c2230
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `pH-Wert` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='368' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.pH-Wert.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Signifikante Wellenhöhe` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='362' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Signifikante Wellenhöhe.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2237c2237
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Fließgeschwindkeit` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='370' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Fließgeschwindkeit.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Maximale Wellenhöhe` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='364' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Maximale Wellenhöhe.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2244c2244
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Windgeschwindigkeit` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='352' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Windgeschwindigkeit.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Trübung` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='350' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Trübung.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2251c2251
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Niederschlagsintensität` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='358' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Niederschlagsintensität.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Durchfahrtshöhe` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='348' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Durchfahrtshöhe.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2258c2258
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Niederschlag` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='356' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Niederschlag.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Elektrische Leitfähigkeit Rohdaten` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='346' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Elektrische Leitfähigkeit Rohdaten.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2265c2265
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Windrichtung` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='354' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Windrichtung.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Abfluss Rohdaten` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='344' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Abfluss Rohdaten.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2272c2272
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Windgeschwindigkeit` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='352' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Windgeschwindigkeit.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Chlorid` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='372' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Chlorid.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2279c2279
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Trübung` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='350' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Trübung.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Wassertemperatur` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='342' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Wassertemperatur.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2286c2286
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Wasserstand Rohdaten` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='338' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Wasserstand Rohdaten.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Fließgeschwindkeit` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='370' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Fließgeschwindkeit.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2293c2293
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Durchfahrtshöhe` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='348' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Durchfahrtshöhe.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Sauerstoffgehalt` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='366' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Sauerstoffgehalt.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2300c2300
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Elektrische Leitfähigkeit Rohdaten` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='346' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Elektrische Leitfähigkeit Rohdaten.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Wasserstand Rohdaten` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='338' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Wasserstand Rohdaten.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2307c2307
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Abfluss Rohdaten` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='344' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Abfluss Rohdaten.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `pH-Wert` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='368' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.pH-Wert.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2314c2314
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Wassertemperatur` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='342' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Wassertemperatur.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Lufttemperatur` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='340' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Lufttemperatur.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2321c2321
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Period` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='382' startCol='9' endLine='385' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[2].content.start.schema.examples.Period.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">380</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Zeitpunkt codiert im [ISO_8601](https://de.wikipedia.org/wiki/ISO_8601) Format. Angabe eines Datums oder einer Period (_P_, z.B. &#39;P8D&#39; für die Messwerte der letzten 8 Tage) sind möglich.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Period` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='382' startCol='9' endLine='385' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[2].content.start.schema.examples.Period.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">380</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Zeitpunkt codiert im [ISO_8601](https://de.wikipedia.org/wiki/ISO_8601) Format. Angabe eines Datums oder einer Period (_P_, z.B. &#39;P8D&#39; für die Messwerte der letzten 8 Tage) sind möglich.&#34;</span><span class="w">
2496c2496
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Kein Filter` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='69' startCol='13' endLine='70' endCol='22' path="$.paths./stations.json.get.parameters[3].content.waters.schema.examples.Kein Filter.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">67</span><span class="cl"><span class="w">          </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Gewässer, filtert Stationen für bestimmte Gewässer&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Rhein` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='69' startCol='13' endLine='73' endCol='17' path="$.paths./stations.json.get.parameters[3].content.waters.schema.examples.Rhein.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">67</span><span class="cl"><span class="w">          </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Gewässer, filtert Stationen für bestimmte Gewässer&#34;</span><span class="w">
2503c2503
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Rhein` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='69' startCol='13' endLine='73' endCol='17' path="$.paths./stations.json.get.parameters[3].content.waters.schema.examples.Rhein.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">67</span><span class="cl"><span class="w">          </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Gewässer, filtert Stationen für bestimmte Gewässer&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Kein Filter` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='69' startCol='13' endLine='70' endCol='22' path="$.paths./stations.json.get.parameters[3].content.waters.schema.examples.Kein Filter.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">67</span><span class="cl"><span class="w">          </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Gewässer, filtert Stationen für bestimmte Gewässer&#34;</span><span class="w">
2517c2517
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Kein Filter` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='91' startCol='13' endLine='92' endCol='22' path="$.paths./stations.json.get.parameters[6].content.fuzzyId.schema.examples.Kein Filter.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">89</span><span class="cl"><span class="w">            </span><span class="nt">type</span><span class="p">:</span><span class="w"> </span><span class="l">string</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Berlin` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='91' startCol='13' endLine='94' endCol='22' path="$.paths./stations.json.get.parameters[6].content.fuzzyId.schema.examples.Berlin.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">89</span><span class="cl"><span class="w">            </span><span class="nt">type</span><span class="p">:</span><span class="w"> </span><span class="l">string</span><span class="w">
2524c2524
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Berlin` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='91' startCol='13' endLine='94' endCol='22' path="$.paths./stations.json.get.parameters[6].content.fuzzyId.schema.examples.Berlin.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">89</span><span class="cl"><span class="w">            </span><span class="nt">type</span><span class="p">:</span><span class="w"> </span><span class="l">string</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Kein Filter` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='91' startCol='13' endLine='92' endCol='22' path="$.paths./stations.json.get.parameters[6].content.fuzzyId.schema.examples.Kein Filter.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">89</span><span class="cl"><span class="w">            </span><span class="nt">type</span><span class="p">:</span><span class="w"> </span><span class="l">string</span><span class="w">
2699c2699
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Pegelnummer` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='314' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[0].content.station.schema.examples.Pegelnummer.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `UUID` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='309' endCol='18' path="$.paths./stations/{station}.json.get.parameters[0].content.station.schema.examples.UUID.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
2706c2706
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Name` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='312' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[0].content.station.schema.examples.Name.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `UUID` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='309' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[0].content.station.schema.examples.UUID.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
2713c2713
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Name` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='312' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[0].content.station.schema.examples.Name.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Name` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='312' endCol='18' path="$.paths./stations/{station}.json.get.parameters[0].content.station.schema.examples.Name.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
2720c2720
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `UUID` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='309' endCol='18' path="$.paths./stations/{station}.json.get.parameters[0].content.station.schema.examples.UUID.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `UUID` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='309' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[0].content.station.schema.examples.UUID.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
2727c2727
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Name` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='312' endCol='18' path="$.paths./stations/{station}.json.get.parameters[0].content.station.schema.examples.Name.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Name` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='312' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[0].content.station.schema.examples.Name.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
2741c2741
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `UUID` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='309' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[0].content.station.schema.examples.UUID.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Pegelnummer` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='314' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[0].content.station.schema.examples.Pegelnummer.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
2762c2762
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Pegelnummer` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='314' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[0].content.station.schema.examples.Pegelnummer.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Pegelnummer` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='314' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[0].content.station.schema.examples.Pegelnummer.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
2769c2769
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `UUID` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='309' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[0].content.station.schema.examples.UUID.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Pegelnummer` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='314' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[0].content.station.schema.examples.Pegelnummer.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
2776c2776
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Pegelnummer` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='314' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[0].content.station.schema.examples.Pegelnummer.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Name` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='308' startCol='9' endLine='312' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[0].content.station.schema.examples.Name.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">306</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;UUID / Name / Pegelnummer der Station.&#34;</span><span class="w">
2783c2783
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Lufttemperatur` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='340' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Lufttemperatur.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Chlorid` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='372' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Chlorid.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2790c2790
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Chlorid` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='372' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Chlorid.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Windrichtung` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='354' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Windrichtung.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2797c2797
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Trübung` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='350' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Trübung.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Chlorid` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='372' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Chlorid.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2804c2804
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Durchfahrtshöhe` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='348' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Durchfahrtshöhe.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Wasserstand Rohdaten` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='338' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Wasserstand Rohdaten.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
2811c2811
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `
eit` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='352' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Windgeschwindigkeit.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3042c3042
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Wellenperiode` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='360' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Wellenperiode.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Windrichtung` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='354' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Windrichtung.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3049c3049
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Signifikante Wellenhöhe` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='362' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Signifikante Wellenhöhe.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Niederschlag` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='356' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Niederschlag.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3056c3056
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Maximale Wellenhöhe` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='364' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Maximale Wellenhöhe.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Niederschlagsintensität` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='358' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Niederschlagsintensität.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3063c3063
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Sauerstoffgehalt` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='366' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Sauerstoffgehalt.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Wellenperiode` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='360' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Wellenperiode.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3070c3070
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `pH-Wert` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='368' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.pH-Wert.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Signifikante Wellenhöhe` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='362' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Signifikante Wellenhöhe.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3077c3077
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Fließgeschwindkeit` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='370' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[1].content.timeseries.schema.examples.Fließgeschwindkeit.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Maximale Wellenhöhe` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='364' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Maximale Wellenhöhe.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3084c3084
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Windgeschwindigkeit` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='352' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Windgeschwindigkeit.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Trübung` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='350' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Trübung.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3091c3091
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Niederschlagsintensität` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='358' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Niederschlagsintensität.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Durchfahrtshöhe` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='348' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Durchfahrtshöhe.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3098c3098
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Niederschlag` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='356' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Niederschlag.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Elektrische Leitfähigkeit Rohdaten` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='346' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Elektrische Leitfähigkeit Rohdaten.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3105c3105
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Windrichtung` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='354' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Windrichtung.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Abfluss Rohdaten` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='344' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Abfluss Rohdaten.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3112c3112
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Windgeschwindigkeit` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='352' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Windgeschwindigkeit.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Chlorid` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='372' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Chlorid.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3119c3119
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Trübung` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='350' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Trübung.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Wassertemperatur` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='342' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Wassertemperatur.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3126c3126
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Wasserstand Rohdaten` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='338' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Wasserstand Rohdaten.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Fließgeschwindkeit` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='370' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Fließgeschwindkeit.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3133c3133
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Durchfahrtshöhe` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='348' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Durchfahrtshöhe.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Sauerstoffgehalt` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='366' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.Sauerstoffgehalt.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3140c3140
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Elektrische Leitfähigkeit Rohdaten` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='346' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Elektrische Leitfähigkeit Rohdaten.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Wasserstand Rohdaten` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='338' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Wasserstand Rohdaten.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3147c3147
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Abfluss Rohdaten` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='344' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Abfluss Rohdaten.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `pH-Wert` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='368' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[1].content.timeseries.schema.examples.pH-Wert.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3154c3154
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Wassertemperatur` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='342' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Wassertemperatur.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Lufttemperatur` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='337' startCol='9' endLine='340' endCol='18' path="$.paths./stations/{station}/{timeseries}.json.get.parameters[1].content.timeseries.schema.examples.Lufttemperatur.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">335</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;timeseries shortname&#34;</span><span class="w">
3161c3161
<                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Period` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='382' startCol='9' endLine='385' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.png.get.parameters[2].content.start.schema.examples.Period.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">380</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Zeitpunkt codiert im [ISO_8601](https://de.wikipedia.org/wiki/ISO_8601) Format. Angabe eines Datums oder einer Period (_P_, z.B. &#39;P8D&#39; für die Messwerte der letzten 8 Tage) sind möglich.&#34;</span><span class="w">
---
>                             <category-rule-result category="examples" howToFix="Examples are critical for consumers to be able to understand schemas and models defined by the spec. Without examples, developers can't understand the type of data the API will return in real life. Examples are turned into mocks and can provide a rich testing capability for APIs. Add detailed examples everywhere!" slot="results" message="Example `Period` missing a `summary` - examples need explaining" ruleId="oas3-valid-schema-example" startLine='382' startCol='9' endLine='385' endCol='18' path="$.paths./stations/{station}/{timeseries}/measurements.json.get.parameters[2].content.start.schema.examples.Period.value"><pre tabindex="0" class="chroma"><code><span class="line"><span class="ln">380</span><span class="cl"><span class="w">      </span><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Zeitpunkt codiert im [ISO_8601](https://de.wikipedia.org/wiki/ISO_8601) Format. Angabe eines Datums oder einer Period (_P_, z.B. &#39;P8D&#39; für die Messwerte der letzten 8 Tage) sind möglich.&#34;</span><span class="w">
